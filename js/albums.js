$(function(){
	// Узнаем количество альбомов начиная с 0.
	var countAlbums = albums.length;
	// Выводим все альбомы.
	console.log(albumHtml);
	divAlbum.append(albumHtml);
	$(divAlbumImgs).on('click', function(){
		divGallery.append()
		divAlbum.remove();
	});
});